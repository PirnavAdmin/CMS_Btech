import { useEffect,useState } from 'react';import{Link,useParams}from'react-router-dom';import DashboardLayout from '../../layouts/DashboardLayout';import{courseApi}from'./courseData';import'./CourseModule.css';
export default function CourseDetails(){const{id}=useParams(),[course,setCourse]=useState(),[branches,setBranches]=useState([]);useEffect(()=>{Promise.all([courseApi.getCourses(),courseApi.getBranches()]).then(([c,b])=>{setCourse(c.find(x=>x.id===id));setBranches(b.filter(x=>x.courseId===id))})},[id]);if(!course)return <DashboardLayout><div className="cm-empty">Course not found.</div></DashboardLayout>;const details=[['Course Code',course.code],['Course Name',course.name],['Short Name',course.shortName],['Course Type',course.type],['Department',course.department],['Qualification',course.qualification],['Academic System',course.academicSystem],['Duration',`${course.durationValue} ${course.durationUnit}`],['Total Semesters',course.semesters],['Status',course.status],['Created Date',course.createdAt],['Last Updated',course.updatedAt]];return <DashboardLayout><div className="cm-page"><header className="cm-header"><div><h1>Course Details</h1><p>{course.name}</p></div><div className="cm-row-actions"><Link className="cm-button secondary" to="/courses">Back</Link><Link className="cm-button" to={`/courses/${id}/edit`}>Edit Course</Link><Link className="cm-button" to={`/branches/add?course=${id}`}>Add Branch</Link></div></header><section className="cm-panel"><h2>Basic Details</h2><div className="cm-details">{details.map(x=><div className="cm-detail" key={x[0]}><span>{x[0]}</span><strong>{x[1]||'—'}</strong></div>)}</div><h2>Additional Information</h2><p><strong>Description:</strong> {course.description||'—'}</p><p><strong>Eligibility:</strong> {course.eligibility||'—'}</p></section><section className="cm-panel cm-table-wrap"><h2>Branch Information</h2><table className="cm-table"><thead><tr><th>Code</th><th>Name</th><th>Duration</th><th>Intake</th><th>Status</th><th>Actions</th></tr></thead><tbody>{branches.map(b=><tr key={b.id}><td>{b.code}</td><td>{b.name}</td><td>{b.durationValue} {b.durationUnit}</td><td>{b.intake}</td><td>{b.status}</td><td><Link className="cm-button secondary small" to={`/courses/${id}/branches/${b.id}/structure`}>Structure</Link></td></tr>)}</tbody></table></section></div></DashboardLayout>}
