import { useEffect, useMemo, useState } from 'react'
import { FiCopy, FiEdit2, FiEye, FiFilter, FiPlus, FiSearch, FiSettings, FiTrash2, FiX } from 'react-icons/fi'
import DashboardLayout from '../../layouts/DashboardLayout'
import { academicYearApi, branchApi, courseApi, departmentApi, courseStructureApi } from '../../api/apiEndpoints'
import { blankAcademic, componentTotals, feeComponent, findConflict, money, persistHostelStructures, persistTransportStructures, readHostelStructures, readStructures, readTransportStructures, saveAcademic, structureCode, structureName, uid } from './feeStructureService'
import './FeeStructure.css'
const PERIODS=['Per Semester','Per Academic Year'], YEARS=['1st Year','2nd Year','3rd Year','4th Year'], ADMISSIONS=['Regular','Lateral Entry','Transfer'], QUOTAS=['Convener','Management','NRI','Sports','Other'], CATEGORIES=['Academic','University','Examination','Laboratory','Administrative','Student Service','Deposit','Miscellaneous'], FREQUENCIES=['One Time','Per Academic Year','Per Semester','Monthly','Custom'], STATUSES=['Draft','Active','Inactive','Archived']
const norm=(rows,ids,names)=>(rows||[]).map(x=>({...x,id:ids.map(k=>x[k]).find(v=>v!=null),name:names.map(k=>x[k]).find(Boolean)})), same=(a,b)=>String(a??'')===String(b??''), required=v=>String(v??'').trim()?'':'Required'
const payable=f=>{const t=componentTotals(f.feeComponents);return t.mandatory+(f.paymentPlan.includeRefundable?t.refundable:0)}

export default function FeeStructure(){
 const[domain,setDomain]=useState('academic'),[rows,setRows]=useState(readStructures),[hostels,setHostels]=useState(readHostelStructures),[transports,setTransports]=useState(readTransportStructures),[masters,setMasters]=useState({years:[],departments:[],courses:[],branches:[],semesters:[]}),[loading,setLoading]=useState(true),[loadError,setLoadError]=useState(''),[form,setForm]=useState(null),[details,setDetails]=useState(null),[query,setQuery]=useState(''),[filters,setFilters]=useState({}),[showFilters,setShowFilters]=useState(false),[toast,setToast]=useState('')
 useEffect(()=>{Promise.all([academicYearApi.getAll(),departmentApi.getAll(),courseApi.getAll(),branchApi.getAll(),courseStructureApi.getAll()]).then(([y,d,c,b,s])=>setMasters({years:norm(y,['academicYearId','id'],['academicYearName','name']),departments:norm(d,['departmentId','id'],['departmentName','name']),courses:norm(c,['courseId','id'],['courseName','name']),branches:norm(b,['branchId','id'],['branchName','name']),semesters:norm(s,['semesterId','structureId','id'],['semesterName','name'])})).catch(e=>setLoadError(e.message||'Unable to load academic masters.')).finally(()=>setLoading(false))},[])
 const notify=m=>{setToast(m);setTimeout(()=>setToast(''),2200)}, filtered=useMemo(()=>rows.filter(x=>`${x.code} ${x.name}`.toLowerCase().includes(query.toLowerCase())&&Object.entries(filters).every(([k,v])=>!v||same(k==='level'?(x.semesterId||x.yearOfStudy):x[k],v))),[rows,query,filters])
 const save=(data,newVersion=false)=>{const result=saveAcademic(rows,data,newVersion);if(result.error)return result.error;setRows(result.rows);setForm(null);notify(newVersion?'New version created.':'Academic fee structure saved.');return''}
 const create=()=>domain==='academic'?setForm(blankAcademic()):domain==='hostel'?setHostels(x=>[...x,blankHostel()]):setTransports(x=>[...x,blankTransport()])
 return <DashboardLayout><main className="fs-page">{toast&&<div className="fs-toast">{toast}</div>}<header className="fs-head"><div><p>Dashboard / Fee Management</p><h1>Fee Structure Configuration</h1><span>Independent academic, hostel and transport financial models.</span></div><button className="primary" onClick={create}><FiPlus/> Create {domain==='academic'?'Academic Structure':domain==='hostel'?'Hostel Plan':'Transport Route'}</button></header><nav className="fs-domain-tabs">{[['academic','Academic Fee Structures'],['hostel','Hostel Fee Structures'],['transport','Transport Fee Structures']].map(([k,v])=><button className={domain===k?'active':''} onClick={()=>setDomain(k)} key={k}>{v}</button>)}</nav>
 {domain==='academic'&&<><div className="fs-filter-actions"><button className={showFilters?'active':''} onClick={()=>setShowFilters(value=>!value)}><FiFilter/> Filter</button><button onClick={()=>{setFilters({});setQuery('')}}>Reset</button></div>{showFilters&&<Filters masters={masters} value={filters} change={setFilters}/>} {loading?<Empty text="Loading academic masters..."/>:loadError?<Empty text={loadError}/>:<List rows={filtered} query={query} setQuery={setQuery} view={setDetails} edit={setForm}/>}</>}
 {domain==='hostel'&&<Facility type="hostel" rows={hostels} masters={masters} persist={x=>{setHostels(x);persistHostelStructures(x)}}/>}{domain==='transport'&&<Facility type="transport" rows={transports} masters={masters} persist={x=>{setTransports(x);persistTransportStructures(x)}}/>}
 {form&&<Editor value={structuredClone(form)} masters={masters} rows={rows} close={()=>setForm(null)} save={save}/>} {details&&<Details row={details} close={()=>setDetails(null)} edit={()=>{setForm(details);setDetails(null)}}/>}</main></DashboardLayout>
}

function Filters({masters,value,change}){const courses=masters.courses.filter(x=>!value.departmentId||same(x.departmentId??x.department?.id,value.departmentId)),branches=masters.branches.filter(x=>!value.courseId||same(x.courseId??x.course?.id,value.courseId));return <section className="fs-filters"><div><MasterSelect label="Academic Year" rows={masters.years} value={value.academicYearId} change={academicYearId=>change({...value,academicYearId})}/><MasterSelect label="Department" rows={masters.departments} value={value.departmentId} change={departmentId=>change({...value,departmentId,courseId:'',branchId:''})}/><MasterSelect label="Course" rows={courses} value={value.courseId} change={courseId=>change({...value,courseId,branchId:''})}/><MasterSelect label="Branch" rows={branches} value={value.branchId} change={branchId=>change({...value,branchId})}/><SimpleSelect label="Fee Period" values={PERIODS} value={value.feePeriod} change={feePeriod=>change({...value,feePeriod})}/><SimpleSelect label="Admission Type" values={ADMISSIONS} value={value.admissionType} change={admissionType=>change({...value,admissionType})}/><SimpleSelect label="Quota" values={QUOTAS} value={value.quota} change={quota=>change({...value,quota})}/><SimpleSelect label="Status" values={STATUSES} value={value.status} change={status=>change({...value,status})}/></div></section>}
function List({rows,query,setQuery,view,edit}){return <section className="fs-card"><header className="fs-list-head"><div><h2>Academic Fee Structures</h2><span>{rows.length} records</span></div><label><FiSearch/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search code or structure"/></label></header>{!rows.length?<Empty text="No academic fee structures match this scope."/>:<div className="fs-table"><table><thead><tr><th>Code / Version</th><th>Structure Name</th><th>Academic Setup</th><th>Fee Period</th><th>Admission / Quota</th><th>Mandatory</th><th>Refundable</th><th>Optional</th><th>Effective Period</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(x=>{const t=componentTotals(x.feeComponents);return <tr key={x.id}><td><strong>{x.code}</strong><small>Version {x.version}</small></td><td>{x.name}</td><td>{x.departmentName}<small>{x.courseName} / {x.branchName}</small></td><td>{x.feePeriod}<small>{x.semesterName||x.yearOfStudy}</small></td><td>{x.admissionType}<small>{x.quota}{x.studentCategory?` / ${x.studentCategory}`:''}</small></td><td>{money(t.mandatory)}</td><td>{money(t.refundable)}</td><td>{money(t.optional)}</td><td>{x.effectiveFrom}<small>{x.effectiveTo||'Open ended'}</small></td><td><Badge value={x.status}/></td><td><button onClick={()=>view(x)}><FiEye/></button><button onClick={()=>edit(x)}><FiEdit2/></button><button onClick={()=>edit({...structuredClone(x),id:'',version:1,status:'Draft'})}><FiCopy/></button></td></tr>})}</tbody></table></div>}</section>}

function Editor({value,masters,rows,close,save}){
 const[f,setF]=useState(value),[step,setStep]=useState(0),[errors,setErrors]=useState({}),[message,setMessage]=useState(''),steps=['Basic Information','Academic Applicability','Fee Components','Payment Schedule','Fine & Concession Rules','Preview'],totals=componentTotals(f.feeComponents),amountPayable=payable(f),p=f.paymentPlan,allocated=p.allocationMode==='Percentage'?p.installments.reduce((a,x)=>a+Number(x.percentage||0),0):p.installments.reduce((a,x)=>a+Number(x.amount||0),0)
 const courses=masters.courses.filter(x=>!f.departmentId||same(x.departmentId??x.department?.id,f.departmentId)),branches=masters.branches.filter(x=>same(x.courseId??x.course?.id,f.courseId)),semesters=masters.semesters.filter(x=>(!f.courseId||same(x.courseId??x.course?.id,f.courseId))&&(!f.branchId||same(x.branchId??x.branch?.id,f.branchId))), patch=(key,v)=>setF({...f,[key]:v}), select=(idKey,nameKey,list,id,resets={})=>{const x=list.find(a=>same(a.id,id));setF({...f,[idKey]:id,[nameKey]:x?.name||'',...resets})}
 const validate=i=>{const e={};if(i===0){e.academicYearId=required(f.academicYearId);e.effectiveFrom=required(f.effectiveFrom);if(f.effectiveFrom&&f.effectiveTo&&f.effectiveTo<f.effectiveFrom)e.effectiveTo='End date cannot precede start date.'}if(i===1){['departmentId','courseId','branchId','feePeriod','admissionType','quota'].forEach(k=>e[k]=required(f[k]));e[f.feePeriod==='Per Semester'?'semesterId':'yearOfStudy']=required(f.feePeriod==='Per Semester'?f.semesterId:f.yearOfStudy)}if(i===2){if(!f.feeComponents.length)e.feeComponents='Add at least one fee component.';f.feeComponents.forEach((x,n)=>{if(!x.name.trim()||Number(x.amount)<=0)e[`component-${n}`]='Fee head and positive amount are required.'});const names=f.feeComponents.map(x=>x.name.trim().toLowerCase()).filter(Boolean);if(new Set(names).size!==names.length)e.feeComponents='Duplicate fee heads are not allowed.'}if(i===3){if(p.mode==='Full Payment')e.dueDate=required(p.dueDate);else{if(!p.installments.length||p.installments.some(x=>!x.name||!x.dueDate||Number(x[p.allocationMode==='Percentage'?'percentage':'amount'])<=0))e.installments='Complete every installment.';if(Math.abs(allocated-(p.allocationMode==='Percentage'?100:amountPayable))>.01)e.installments=`Allocation must equal ${p.allocationMode==='Percentage'?'100%':money(amountPayable)}.`}}if(i===4&&f.fineRules.type!=='No Fine'&&Number(f.fineRules.value)<=0)e.fineValue='Enter a positive fine value.';return Object.fromEntries(Object.entries(e).filter(([,v])=>v))}
 const go=n=>{if(n>step){let e={};for(let i=step;i<n;i++)e={...e,...validate(i)};if(Object.keys(e).length){setErrors(e);setMessage('Complete each preceding step before continuing.');return}}setErrors({});setMessage('');setStep(n)}, submit=(status,newVersion=false)=>{let e={};for(let i=0;i<5;i++)e={...e,...validate(i)};const candidate={...f,status};if(status==='Active'){const conflict=findConflict(rows,candidate);if(conflict)e.status=`Effective dates overlap ${conflict.code}.`}if(Object.keys(e).length){setErrors(e);setMessage(Object.values(e)[0]);return}setMessage(save(candidate,newVersion)||'')}, historical=Number(f.assignedCount)>0
 return <div className="fs-overlay"><section className="fs-editor"><header><div><span>ACADEMIC FEE STRUCTURE</span><h2>{f.id?'Edit':'Create'} Academic Fee Structure</h2><p>{[f.academicYearName,f.courseName,f.branchName].filter(Boolean).join(' / ')||'Configure a precise applicability scope.'}</p></div><button onClick={close}><FiX/></button></header><nav>{steps.map((x,i)=><button key={x} className={step===i?'active':i<step&&!Object.keys(validate(i)).length?'done':''} onClick={()=>go(i)}><i>{i<step?'Done':i+1}</i>{x}</button>)}</nav><div className="fs-editor-body">
 {step===0&&<><StepTitle title="Basic Information" text="Lifecycle, version and effective period."/><div className="fs-grid"><MasterField label="Academic Year *" error={errors.academicYearId} rows={masters.years} value={f.academicYearId} change={v=>select('academicYearId','academicYearName',masters.years,v)}/><Field label="Version"><input readOnly value={f.version}/></Field><Field label="Effective From *" error={errors.effectiveFrom}><input type="date" value={f.effectiveFrom} onChange={e=>patch('effectiveFrom',e.target.value)}/></Field><Field label="Effective To" error={errors.effectiveTo}><input type="date" min={f.effectiveFrom} value={f.effectiveTo} onChange={e=>patch('effectiveTo',e.target.value)}/></Field><SimpleField label="Status" values={STATUSES} value={f.status} change={v=>patch('status',v)}/></div>{historical&&<p className="fs-warning">Assigned to {f.assignedCount} student(s). Create a new version to preserve history.</p>}</>}
 {step===1&&<><StepTitle title="Academic Applicability" text="Parent changes reset invalid child selections."/><div className="fs-grid"><MasterField label="Department *" error={errors.departmentId} rows={masters.departments} value={f.departmentId} change={v=>select('departmentId','departmentName',masters.departments,v,{courseId:'',courseName:'',branchId:'',branchName:'',semesterId:'',semesterName:''})}/><MasterField label="Course *" error={errors.courseId} disabled={!f.departmentId} rows={courses} value={f.courseId} change={v=>select('courseId','courseName',courses,v,{branchId:'',branchName:'',semesterId:'',semesterName:''})}/><MasterField label="Branch *" error={errors.branchId} disabled={!f.courseId} rows={branches} value={f.branchId} change={v=>select('branchId','branchName',branches,v,{semesterId:'',semesterName:''})}/><SimpleField label="Fee Period *" error={errors.feePeriod} values={PERIODS} value={f.feePeriod} change={v=>setF({...f,feePeriod:v,semesterId:'',semesterName:'',yearOfStudy:''})}/>{f.feePeriod==='Per Semester'?<MasterField label="Semester *" error={errors.semesterId} rows={semesters} value={f.semesterId} change={v=>select('semesterId','semesterName',semesters,v)}/>:<SimpleField label="Year of Study *" error={errors.yearOfStudy} values={YEARS} value={f.yearOfStudy} change={v=>patch('yearOfStudy',v)}/>}<SimpleField label="Admission Type *" error={errors.admissionType} values={ADMISSIONS} value={f.admissionType} change={v=>patch('admissionType',v)}/><SimpleField label="Quota *" error={errors.quota} values={QUOTAS} value={f.quota} change={v=>patch('quota',v)}/><SimpleField label="Student Category" values={['General','SC','ST','BC','EWS','Other']} value={f.studentCategory} change={v=>patch('studentCategory',v)} empty="All Categories"/></div></>}
 {step===2&&<Components f={f} setF={setF} errors={errors} totals={totals} amountPayable={amountPayable}/>} {step===3&&<Payment f={f} setF={setF} amountPayable={amountPayable} allocated={allocated} error={errors.installments||errors.dueDate}/>} {step===4&&<Rules f={f} setF={setF} error={errors.fineValue}/>} {step===5&&<Preview f={f}/>} {message&&<p className="error">{message}</p>}</div><footer><button disabled={!step} onClick={()=>go(step-1)}>Back</button><span/>{step<5?<button className="primary" onClick={()=>go(step+1)}>Continue</button>:<>{historical&&<button onClick={()=>submit('Active',true)}>Create New Version</button>}<button onClick={()=>submit('Draft')}>Save as Draft</button><button className="primary" onClick={()=>submit('Active')}>Activate</button></>}</footer></section></div>}

function Components({f,setF,errors,totals,amountPayable}){const update=(id,k,v)=>setF({...f,feeComponents:f.feeComponents.map(x=>x.id===id?{...x,[k]:v}:x)});return <><StepTitle title="Fee Components" text="One normalized source of truth for all academic charges."/><button className="primary" onClick={()=>setF({...f,feeComponents:[...f.feeComponents,feeComponent()]})}><FiPlus/> Add Fee Component</button>{errors.feeComponents&&<p className="error">{errors.feeComponents}</p>}<div className="fs-fee-components">{f.feeComponents.map((x,i)=><div key={x.id} className={errors[`component-${i}`]?'invalid-row':''}><input placeholder="Fee Head *" value={x.name} onChange={e=>update(x.id,'name',e.target.value)}/><select value={x.category} onChange={e=>update(x.id,'category',e.target.value)}>{CATEGORIES.map(v=><option key={v}>{v}</option>)}</select><input type="number" min="0" placeholder="Amount *" value={x.amount} onChange={e=>update(x.id,'amount',e.target.value)}/><select value={x.frequency} onChange={e=>update(x.id,'frequency',e.target.value)}>{FREQUENCIES.map(v=><option key={v}>{v}</option>)}</select><select value={x.requirement} onChange={e=>update(x.id,'requirement',e.target.value)}><option>Mandatory</option><option>Optional</option></select><select value={x.refundable} onChange={e=>update(x.id,'refundable',e.target.value)}><option>Non-refundable</option><option>Refundable</option></select><label><input type="checkbox" checked={x.concessionAllowed} onChange={e=>update(x.id,'concessionAllowed',e.target.checked)}/> Concession</label><label><input type="checkbox" checked={x.fineApplicable} onChange={e=>update(x.id,'fineApplicable',e.target.checked)}/> Fine</label><input placeholder="Due rule" value={x.dueRule} onChange={e=>update(x.id,'dueRule',e.target.value)}/><select value={x.status} onChange={e=>update(x.id,'status',e.target.value)}><option>Active</option><option>Inactive</option></select><button onClick={()=>setF({...f,feeComponents:f.feeComponents.filter(c=>c.id!==x.id)})}><FiTrash2/></button></div>)}</div><Totals totals={totals} amountPayable={amountPayable}/></>}
function Payment({f,setF,amountPayable,allocated,error}){const p=f.paymentPlan,set=v=>setF({...f,paymentPlan:{...p,...v}}),update=(id,k,v)=>set({installments:p.installments.map(x=>x.id===id?{...x,[k]:v}:x)}),allocationKey=p.allocationMode==='Percentage'?'percentage':'amount';return <><StepTitle title="Payment Schedule" text="Allocate mandatory payable; refundable deposits are opt-in."/><div className="fs-grid"><SimpleField label="Payment Mode" values={['Full Payment','Installments']} value={p.mode} change={mode=>set({mode})}/><Toggle label="Include refundable deposits" checked={p.includeRefundable} change={includeRefundable=>set({includeRefundable})}/>{p.mode==='Full Payment'?<Field label="Due Date *" error={error}><input type="date" value={p.dueDate} onChange={e=>set({dueDate:e.target.value})}/></Field>:<SimpleField label="Allocation Mode" values={['Amount','Percentage']} value={p.allocationMode} change={allocationMode=>set({allocationMode})}/>}</div>{p.mode==='Installments'&&<><button className="primary" onClick={()=>set({installments:[...p.installments,{id:uid('IN'),name:`Installment ${p.installments.length+1}`,amount:'',percentage:'',dueDate:'',gracePeriod:'',fineRule:'Use Structure Rule'}]})}><FiPlus/> Add Installment</button>{p.installments.map(x=><div className="fs-installment" key={x.id}><input placeholder="Name" value={x.name} onChange={e=>update(x.id,'name',e.target.value)}/><input type="number" min="0" placeholder={p.allocationMode} value={x[allocationKey]} onChange={e=>update(x.id,allocationKey,e.target.value)}/><input type="date" value={x.dueDate} onChange={e=>update(x.id,'dueDate',e.target.value)}/><input type="number" min="0" placeholder="Grace days" value={x.gracePeriod} onChange={e=>update(x.id,'gracePeriod',e.target.value)}/><select value={x.fineRule} onChange={e=>update(x.id,'fineRule',e.target.value)}><option>Use Structure Rule</option><option>No Fine</option></select><button onClick={()=>set({installments:p.installments.filter(i=>i.id!==x.id)})}><FiTrash2/></button></div>)}</>}<div className="fs-allocation"><span>Mandatory Payable<strong>{money(amountPayable)}</strong></span><span>Allocated<strong>{p.allocationMode==='Percentage'?`${allocated}%`:money(allocated)}</strong></span><span>Remaining<strong>{p.allocationMode==='Percentage'?`${100-allocated}%`:money(amountPayable-allocated)}</strong></span></div>{error&&<p className="error">{error}</p>}</>}
function Rules({f,setF,error}){const r=f.fineRules,set=v=>setF({...f,fineRules:{...r,...v}});return <><StepTitle title="Fine & Concession Rules" text="Reusable policy only; student concessions belong to the concession module."/><div className="fs-grid"><SimpleField label="Fine Rule" values={['No Fine','Fixed','Per Day','Percentage','Slab Based']} value={r.type} change={type=>set({type})}/>{r.type!=='No Fine'&&<><Field label="Grace Period"><input type="number" min="0" value={r.gracePeriod} onChange={e=>set({gracePeriod:e.target.value})}/></Field><Field label="Fine Value" error={error}><input type="number" min="0" value={r.value} onChange={e=>set({value:e.target.value})}/></Field><Field label="Maximum Fine"><input type="number" min="0" value={r.maximumFine} onChange={e=>set({maximumFine:e.target.value})}/></Field></>}<Toggle label="Allow concession on eligible fee heads" checked={f.concessionPolicy.allowed} change={allowed=>setF({...f,concessionPolicy:{...f.concessionPolicy,allowed}})}/></div><div className="fs-check-list">{f.feeComponents.map(x=><label key={x.id}><input type="checkbox" checked={r.applicableComponentIds.includes(x.id)} onChange={e=>set({applicableComponentIds:e.target.checked?[...r.applicableComponentIds,x.id]:r.applicableComponentIds.filter(id=>id!==x.id)})}/>{x.name||'Unnamed fee head'}</label>)}</div></>}
function Preview({f}){const t=componentTotals(f.feeComponents);return <><StepTitle title="Fee Structure Preview" text="Complete applicability and financial configuration."/><dl className="fs-preview">{[['Structure Name',structureName(f)],['Code',structureCode(f)],['Version',f.version],['Status',f.status],['Academic Year',f.academicYearName],['Department',f.departmentName],['Course / Branch',`${f.courseName} / ${f.branchName}`],['Fee Period',f.feePeriod],['Year / Semester',f.semesterName||f.yearOfStudy],['Admission / Quota',`${f.admissionType} / ${f.quota}`],['Student Category',f.studentCategory||'All Categories'],['Effective Dates',`${f.effectiveFrom} to ${f.effectiveTo||'Open ended'}`]].map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v||'—'}</dd></div>)}</dl><FeeTable rows={f.feeComponents}/><Totals totals={t} amountPayable={payable(f)}/><h3>Payment Schedule</h3><p>{f.paymentPlan.mode}{f.paymentPlan.mode==='Full Payment'?` · Due ${f.paymentPlan.dueDate}`:` · ${f.paymentPlan.installments.length} installments`}</p><h3>Fine Rules</h3><p>{f.fineRules.type} · Concession {f.concessionPolicy.allowed?'allowed':'not allowed'}</p></>}
function Details({row,close,edit}){const[tab,setTab]=useState('Overview'),tabs=['Overview','Fee Components','Payment Schedule','Fine Rules','Versions','Students Assigned'];return <div className="fs-overlay"><section className="fs-editor details"><header><div><span>FEE STRUCTURE DETAILS</span><h2>{row.name}</h2><p>{row.code} · Version {row.version}</p></div><button onClick={close}><FiX/></button></header><nav>{tabs.map(x=><button className={tab===x?'active':''} onClick={()=>setTab(x)} key={x}>{x}</button>)}</nav><div className="fs-editor-body">{tab==='Overview'&&<Preview f={row}/>} {tab==='Fee Components'&&<FeeTable rows={row.feeComponents}/>} {tab==='Payment Schedule'&&<pre className="fs-json">{JSON.stringify(row.paymentPlan,null,2)}</pre>} {tab==='Fine Rules'&&<pre className="fs-json">{JSON.stringify({fineRules:row.fineRules,concessionPolicy:row.concessionPolicy},null,2)}</pre>} {tab==='Versions'&&<Empty text={`Current version ${row.version}; every version has its own immutable ID.`}/>} {tab==='Students Assigned'&&<Empty text={row.assignedCount?`${row.assignedCount} student(s) assigned.`:'No assignment data available.'}/>}</div><footer><button onClick={close}>Back</button><span/><button className="primary" onClick={edit}><FiEdit2/> Edit</button></footer></section></div>}

const blankHostel=()=>({id:uid('HF'),academicYearId:'',academicYearName:'',hostelName:'',hostelType:'Boys Hostel',roomType:'Double Sharing',acType:'Non-AC',messPlan:'Standard',componentsText:'',fee:'',effectiveFrom:'',effectiveTo:'',status:'Draft'}), blankTransport=()=>({id:uid('TF'),academicYearId:'',academicYearName:'',route:'',routeCode:'',pickupPoint:'',distance:'',fee:'',frequency:'Per Academic Year',effectiveFrom:'',effectiveTo:'',status:'Draft'})
function Facility({type,rows,masters,persist}){const hostel=type==='hostel',update=(id,k,v)=>persist(rows.map(x=>x.id===id?{...x,[k]:v}:x));return <section className="fs-card"><header><div><h2>{hostel?'Hostel Fee Structures':'Transport Fee Structures'}</h2><small>Independent from course and branch.</small></div></header>{!rows.length?<Empty text={`No ${hostel?'hostel plans':'transport routes'} configured.`}/>:<div className="fs-facility-list">{rows.map(x=><article key={x.id}><MasterField label="Academic Year" rows={masters.years} value={x.academicYearId} change={v=>{const y=masters.years.find(a=>same(a.id,v));persist(rows.map(r=>r.id===x.id?{...r,academicYearId:v,academicYearName:y?.name||''}:r))}}/>{hostel?<><Field label="Hostel"><input value={x.hostelName} onChange={e=>update(x.id,'hostelName',e.target.value)}/></Field><SimpleField label="Hostel Type" values={['Boys Hostel','Girls Hostel','Co-ed Hostel']} value={x.hostelType} change={v=>update(x.id,'hostelType',v)}/><SimpleField label="Room Type" values={['Single Sharing','Double Sharing','Triple Sharing','Four Sharing','Dormitory']} value={x.roomType} change={v=>update(x.id,'roomType',v)}/><SimpleField label="AC Type" values={['Non-AC','AC']} value={x.acType} change={v=>update(x.id,'acType',v)}/><Field label="Mess Plan"><input value={x.messPlan} onChange={e=>update(x.id,'messPlan',e.target.value)}/></Field><Field label="Components"><input placeholder="Room Rent, Mess, Deposit" value={x.componentsText} onChange={e=>update(x.id,'componentsText',e.target.value)}/></Field></>:<><Field label="Route Code"><input value={x.routeCode} onChange={e=>update(x.id,'routeCode',e.target.value)}/></Field><Field label="Route"><input value={x.route} onChange={e=>update(x.id,'route',e.target.value)}/></Field><Field label="Pickup Point"><input value={x.pickupPoint} onChange={e=>update(x.id,'pickupPoint',e.target.value)}/></Field><Field label="Distance (km)"><input type="number" min="0" value={x.distance} onChange={e=>update(x.id,'distance',e.target.value)}/></Field><SimpleField label="Frequency" values={FREQUENCIES} value={x.frequency} change={v=>update(x.id,'frequency',v)}/></>}<Field label="Fee"><input type="number" min="0" value={x.fee} onChange={e=>update(x.id,'fee',e.target.value)}/></Field><Field label="Effective From"><input type="date" value={x.effectiveFrom} onChange={e=>update(x.id,'effectiveFrom',e.target.value)}/></Field><Field label="Effective To"><input type="date" value={x.effectiveTo} onChange={e=>update(x.id,'effectiveTo',e.target.value)}/></Field><SimpleField label="Status" values={STATUSES} value={x.status} change={v=>update(x.id,'status',v)}/><button onClick={()=>persist(rows.filter(r=>r.id!==x.id))}><FiTrash2/></button></article>)}</div>}</section>}

function FeeTable({rows}){return <div className="fs-table"><table><thead><tr><th>Fee Head</th><th>Category</th><th>Frequency</th><th>Requirement</th><th>Refundability</th><th>Amount</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.category}</td><td>{x.frequency}</td><td>{x.requirement}</td><td>{x.refundable}</td><td>{money(x.amount)}</td></tr>)}</tbody></table></div>}
const Totals=({totals,amountPayable})=><section className="fs-totals"><div><span>Mandatory Academic</span><strong>{money(totals.mandatory)}</strong></div><div><span>Refundable Deposit</span><strong>{money(totals.refundable)}</strong></div><div><span>Optional Academic</span><strong>{money(totals.optional)}</strong></div><div><span>Configured Payable</span><strong>{money(amountPayable)}</strong></div></section>
const Badge=({value})=><span className={`fs-badge ${String(value).toLowerCase()}`}>{value}</span>, Empty=({text})=><div className="fs-empty"><FiSettings/><h3>{text}</h3></div>, StepTitle=({title,text})=><header className="fs-step-title"><h3>{title}</h3><p>{text}</p></header>
const Field=({label,error,children})=>{const isRequired=label.trim().endsWith('*'),text=label.replace(/\s*\*$/,'');return <label className={`fs-field ${error?'invalid':''}`}><span>{text}{isRequired&&<b className="required-mark"> *</b>}</span>{children}{error&&<small>{error}</small>}</label>}, MasterField=({label,error,rows,value,change,disabled})=><Field label={label} error={error}><select disabled={disabled} value={value} onChange={e=>change(e.target.value)}><option value="">Select</option>{rows.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></Field>, SimpleField=({label,error,values,value,change,empty='Select'})=><Field label={label} error={error}><select value={value} onChange={e=>change(e.target.value)}><option value="">{empty}</option>{values.map(x=><option key={x}>{x}</option>)}</select></Field>
const MasterSelect=({label,rows,value,change})=><label>{label}<select value={value||''} onChange={e=>change(e.target.value)}><option value="">All {label}</option>{rows.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>, SimpleSelect=({label,values,value,change})=><label>{label}<select value={value||''} onChange={e=>change(e.target.value)}><option value="">All {label}</option>{values.map(x=><option key={x}>{x}</option>)}</select></label>, Toggle=({label,checked,change})=><label className="fs-toggle"><input type="checkbox" checked={checked} onChange={e=>change(e.target.checked)}/><span>{label}</span></label>
