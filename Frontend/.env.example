# Leave empty during local development to use the Vite /api proxy.
# For a separately hosted API, use a value such as https://api.example.com.
VITE_API_BASE_URL=

# Optional. The current backend does not expose a registration endpoint.
VITE_REGISTRATION_API_URL=
